import { Inter, Cormorant, Caveat } from 'next/font/google'

export const inter = Inter({ 
    subsets: ['latin'], 
    display: 'swap',
})

export const cormorant = Cormorant({
  subsets: ['latin'],
  display: 'swap',
})

export const caveat = Caveat({
  subsets: ['latin'],
  display: 'swap',
})